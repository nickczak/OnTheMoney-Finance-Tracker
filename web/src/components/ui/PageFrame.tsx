/**
 * Fixed engraved plate frame (kit's `.page-frame`). The border only —
 * corner ornaments have been removed from the design.
 */
export default function PageFrame() {
  return <div className="page-frame" aria-hidden />;
}
